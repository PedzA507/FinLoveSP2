<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <corners android:radius="16dp" /> <!-- มุมเหลี่ยม -->
    <stroke
        android:width="2dp"
        android:color="#000000" /> <!-- ขอบสีดำ -->
</shape>

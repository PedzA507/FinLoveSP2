<resources>
    <!-- Base application theme -->
    <style name="Theme.Finlove" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/pink</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/purple_200</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Theme without Action Bar and Status Bar -->
    <style name="Theme.Finlove.NoActionBar" parent="Theme.Finlove">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item> <!-- ซ่อน Status Bar -->
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- ทำให้ Status Bar โปร่งใส -->
    </style>
</resources>
